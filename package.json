{
  "name": "@tsonic/tsts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript-native TypeScript compiler targeting TS-Go contracts",
  "type": "module",
  "bin": {
    "tsts": "./dist/src/cli/tsts.js"
  },
  "exports": {
    ".": "./dist/src/index.js"
  },
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "generate": "tsx tools/generate-ast.ts && tsx tools/generate-diagnostics.ts",
    "schema:check": "tsx tools/check-schema.ts && tsx tools/check-typescript-diagnostics.ts",
    "generated:check": "tsx tools/check-generated.ts",
    "conformance:diagnostics": "tsx tools/run-upstream-diagnostic-conformance.ts",
    "test": "tsx --test test/**/*.test.ts",
    "verify": "npm run schema:check && npm run generate && npm run generated:check && npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
