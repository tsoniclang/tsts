{
  "schemaVersion": 2,
  "projects": [
    {
      "name": "closed-project",
      "cwd": "project",
      "args": ["-p", "tsconfig.json", "--noEmit", "--incremental", "false"],
      "inputs": [
        {
          "label": "complete-project-tree",
          "source": "fixtures/lib-micro",
          "destination": "project",
          "replicas": 1
        }
      ]
    }
  ]
}
