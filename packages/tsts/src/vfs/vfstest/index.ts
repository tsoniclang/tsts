/**
 * Test-only filesystem builder.
 *
 * Port of TS-Go `internal/vfs/vfstest/`. Provides `fromMap` to
 * construct an FS from a plain JS object of paths to contents.
 */

export * from "./vfsTest.js";
