import type { Version$Storage as Version__from_semver$Storage } from "../semver/package.js";
export class $PackageState {
    declare typeScriptVersion: Version__from_semver$Storage;
    declare private readonly then?: never;
}
export const $state = new $PackageState();
