import { hashObjectIdentity } from "./object-identity.js";
export function location(initial) {
    return {
        storageIdentity: {},
        storageKey: undefined,
        value: initial,
    };
}
export function boundLocation(identity, read, write) {
    return new BoundLocation(identity, read, write);
}
class BoundLocation {
    storageIdentity;
    read;
    write;
    storageKey = undefined;
    constructor(storageIdentity, read, write) {
        this.storageIdentity = storageIdentity;
        this.read = read;
        this.write = write;
    }
    get value() {
        return this.read();
    }
    set value(value) {
        this.write(value);
    }
}
export function propertyLocation(object, key) {
    return new PropertyLocation(object, key);
}
class PropertyLocation {
    object;
    key;
    storageIdentity;
    storageKey;
    constructor(object, key) {
        this.object = object;
        this.key = key;
        this.storageIdentity = object;
        this.storageKey = key;
    }
    get value() {
        return this.object[this.key];
    }
    set value(value) {
        this.object[this.key] = value;
    }
}
const childStorageIdentities = new WeakMap();
export function nestedPropertyLocation(parent, key) {
    return new NestedPropertyLocation(parent, key);
}
class NestedPropertyLocation {
    parent;
    key;
    storageIdentity;
    storageKey;
    constructor(parent, key) {
        this.parent = parent;
        this.key = key;
        this.storageIdentity = locationIdentity(parent);
        this.storageKey = key;
    }
    get value() {
        return this.object()[this.key];
    }
    set value(value) {
        this.object()[this.key] = value;
    }
    object() {
        const object = this.parent.value;
        if (object === null || object === undefined) {
            throw new TypeError("cannot access a property through a nullish location");
        }
        return object;
    }
}
function locationIdentity(location) {
    if (location.storageKey === undefined) {
        return location.storageIdentity;
    }
    let children = childStorageIdentities.get(location.storageIdentity);
    if (children === undefined) {
        children = new Map();
        childStorageIdentities.set(location.storageIdentity, children);
    }
    let identity = children.get(location.storageKey);
    if (identity === undefined) {
        identity = {};
        children.set(location.storageKey, identity);
    }
    return identity;
}
export function sameLocation(left, right) {
    if (left === undefined || right === undefined) {
        return left === right;
    }
    return left === right ||
        left.storageIdentity === right.storageIdentity &&
            left.storageKey === right.storageKey;
}
export function hashLocation(pointer) {
    if (pointer === undefined) {
        return 0;
    }
    const identity = locationIdentity(pointer);
    return hashObjectIdentity(identity);
}
export function projectLocation(pointer, fromSource, toSource) {
    if (pointer === undefined) {
        return undefined;
    }
    return new ProjectedLocation(pointer, fromSource, toSource);
}
class ProjectedLocation {
    pointer;
    fromSource;
    toSource;
    storageIdentity;
    storageKey;
    constructor(pointer, fromSource, toSource) {
        this.pointer = pointer;
        this.fromSource = fromSource;
        this.toSource = toSource;
        this.storageIdentity = pointer.storageIdentity;
        this.storageKey = pointer.storageKey;
    }
    get value() {
        return this.fromSource(this.pointer.value);
    }
    set value(value) {
        this.pointer.value = this.toSource(value);
    }
}
//# sourceMappingURL=location.js.map