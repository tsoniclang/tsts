import { GoPanic } from "@gotots/runtime/panic.js";
import { BufferedReaderState, BufferedWriterState, } from "./internal/portable/io/buffer.js";
import { state as ioState } from "./io.js";
import { ProviderError } from "./internal/runtime/error.js";
import { ProviderScanner } from "./internal/portable/io/scanner.js";
class BufferedReader {
    readOperation;
    readByteOperation;
    readBytesOperation;
    constructor(readOperation, readByteOperation, readBytesOperation) {
        this.readOperation = readOperation;
        this.readByteOperation = readByteOperation;
        this.readBytesOperation = readBytesOperation;
    }
    Read(destination) {
        return this.readOperation(destination);
    }
    ReadByte() {
        return this.readByteOperation();
    }
    ReadBytes(delimiter) {
        return this.readBytesOperation(delimiter);
    }
}
export const Reader = Object.freeze({
    Read(receiver, destination) {
        return requireReader(receiver).Read(destination);
    },
    ReadByte(receiver) {
        return requireReader(receiver).ReadByte();
    },
    ReadBytes(receiver, delimiter) {
        return requireReader(receiver).ReadBytes(delimiter);
    },
});
function requireReader(receiver) {
    if (receiver === undefined) {
        GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    return receiver;
}
export class Scanner {
    #implementation;
    constructor(source) {
        this.#implementation = new ProviderScanner(source, state.ErrBadReadCount, state.ErrTooLong, ioState.EOF, ioState.ErrNoProgress);
    }
    static Err(receiver) {
        return requireScanner(receiver).#implementation.Err();
    }
    static Scan(receiver) {
        return requireScanner(receiver).#implementation.Scan();
    }
    static Text(receiver) {
        return requireScanner(receiver).#implementation.Text();
    }
}
function requireScanner(receiver) {
    if (receiver === undefined) {
        GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    return receiver;
}
export const state = {
    ErrBadReadCount: new ProviderError("bufio.Scanner: Read returned impossible count"),
    ErrTooLong: new ProviderError("bufio.Scanner: token too long"),
};
class BufferedWriter {
    flushOperation;
    writeOperation;
    writeByteOperation;
    constructor(flushOperation, writeOperation, writeByteOperation) {
        this.flushOperation = flushOperation;
        this.writeOperation = writeOperation;
        this.writeByteOperation = writeByteOperation;
    }
    Flush() {
        return this.flushOperation();
    }
    Write(source) {
        return this.writeOperation(source);
    }
    WriteByte(value) {
        return this.writeByteOperation(value);
    }
}
export const Writer = Object.freeze({
    Flush(receiver) {
        return requireWriter(receiver).Flush();
    },
    Write(receiver, source) {
        return requireWriter(receiver).Write(source);
    },
    WriteByte(receiver, value) {
        return requireWriter(receiver).WriteByte(value);
    },
});
function requireWriter(receiver) {
    if (receiver === undefined) {
        GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    return receiver;
}
export function NewReader(source) {
    if (source === undefined) {
        return undefined;
    }
    const state = new BufferedReaderState(source);
    return new BufferedReader((destination) => state.Read(destination), () => state.ReadByte(), (delimiter) => state.ReadBytes(delimiter));
}
export function NewScanner(source) {
    return new Scanner(source);
}
export function NewWriter(target) {
    if (target === undefined) {
        return undefined;
    }
    const state = new BufferedWriterState(target);
    return new BufferedWriter(() => state.Flush(), (source) => state.Write(source), (value) => state.WriteByte(value));
}
