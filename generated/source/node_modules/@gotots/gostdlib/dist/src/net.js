import { ProviderError } from "./internal/runtime/error.js";
export function Listen(network, _address) {
    return [
        undefined,
        new ProviderError(`net.Listen ${network} is not implemented by this backend`),
    ];
}
