import { booleanFlag, initializeFlagSet, parseFlags, stringFlag, } from "./internal/node/flag/flag-set.js";
export class ErrorHandling {
    value;
    constructor(value) {
        this.value = value;
    }
}
export const ContinueOnError = new ErrorHandling(0n);
export class FlagSet {
    Usage = undefined;
    static Bool(receiver, name, value, usage) {
        void usage;
        return booleanFlag(receiver, name, value);
    }
    static Parse(receiver, args) {
        return parseFlags(receiver, args);
    }
    static String(receiver, name, value, usage) {
        void usage;
        return stringFlag(receiver, name, value);
    }
}
export function NewFlagSet(name, errorHandling) {
    const set = new FlagSet();
    initializeFlagSet(set, name, errorHandling);
    return set;
}
