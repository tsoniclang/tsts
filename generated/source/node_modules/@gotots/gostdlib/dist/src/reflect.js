import { GoPanic } from "@gotots/runtime/panic.js";
import { hostInteger } from "./internal/host-integer.js";
import { getStructTag, lookupStructTag, } from "./internal/portable/reflect/struct-tag.js";
import { ProviderError } from "./internal/runtime/error.js";
import { providerPlaceholder } from "./internal/runtime/placeholder.js";
import { pointerDescriptorFor, recordRuntimeType, resolveRuntimeType, runtimeValueOperations, } from "./internal/portable/reflect/runtime-value.js";
export class ChanDir {
    value;
    constructor(value) {
        this.value = value;
    }
}
export class Kind {
    value;
    constructor(value) {
        this.value = value;
    }
    String() {
        return kindNames[hostInteger(this.value)] ?? `Kind(${this.value})`;
    }
}
export const Invalid = new Kind(0n);
export const Bool = new Kind(1n);
export const Int = new Kind(2n);
export const Int8 = new Kind(3n);
export const Int16 = new Kind(4n);
export const Int32 = new Kind(5n);
export const Int64 = new Kind(6n);
export const Uint = new Kind(7n);
export const Uint8 = new Kind(8n);
export const Uint16 = new Kind(9n);
export const Uint32 = new Kind(10n);
export const Uint64 = new Kind(11n);
export const Uintptr = new Kind(12n);
export const Float32 = new Kind(13n);
export const Float64 = new Kind(14n);
export const Complex64 = new Kind(15n);
export const Complex128 = new Kind(16n);
export const Array = new Kind(17n);
export const Chan = new Kind(18n);
export const Func = new Kind(19n);
export const Interface = new Kind(20n);
export const Map = new Kind(21n);
export const Pointer = new Kind(22n);
export const Ptr = Pointer;
export const Slice = new Kind(23n);
export const String = new Kind(24n);
export const Struct = new Kind(25n);
export const UnsafePointer = new Kind(26n);
export class Value {
    stored;
    location;
    addressable;
    staticType;
    constructor(stored, location, addressable = false, staticType) {
        this.stored = stored;
        this.location = location;
        this.addressable = addressable;
        this.staticType = staticType;
    }
    // $unbox exposes the canonical interface box to generated support
    // code (type assertions select exact adapters emitter-side).
    $unbox() {
        return this.source;
    }
    // source reads live through the location so mutations that replace the
    // represented value (Set, SetLen, Grow, SetBytes) stay visible to every
    // subsequent read of the same Value, exactly like Go's pointer-backed
    // addressable values.
    get source() {
        return this.location === undefined
            ? this.stored
            : this.location.get();
    }
    static located(location) {
        return new LocatedValue(location.get(), location, true);
    }
    static $append(target, values) {
        const operation = target.operations()?.append;
        if (operation === undefined || target.source === undefined) {
            return GoPanic.raise(new ProviderError(`reflect: call of reflect.Append on ${target.kindText()} Value`));
        }
        const boxes = [];
        for (let index = 0; index < values.length; index++) {
            const element = values.get(index);
            const box = element.source;
            if (!element.IsValid()) {
                return GoPanic.raise(new ProviderError("reflect: call of reflect.Append on zero Value"));
            }
            boxes.push(box);
        }
        return new InterfaceValue(operation(target.source, boxes));
    }
    resolvedType() {
        if (this.staticType !== undefined) {
            return this.staticType;
        }
        if (this.location !== undefined) {
            return this.location.type();
        }
        return this.source === undefined
            ? undefined
            : resolveRuntimeType(this.source);
    }
    operations() {
        return runtimeValueOperations(this.resolvedType());
    }
    kindText() {
        const type = this.resolvedType();
        return type === undefined ? "zero" : `${type.Kind().String()}`;
    }
    operationPanic(operation) {
        return GoPanic.raise(new ProviderError(`reflect: call of reflect.Value.${operation} on ${this.kindText()} Value`));
    }
    Addr() {
        if (!this.addressable || this.location === undefined) {
            return GoPanic.raise(new ProviderError("reflect.Value.Addr of unaddressable value"));
        }
        const pointerType = pointerDescriptorFor(this.location.type());
        const address = this.location.address;
        if (pointerType === undefined || address === undefined) {
            return GoPanic.raise(new ProviderError(`reflect.Value.Addr requires a generated address facet for ${this.location.type().String()}`));
        }
        const pointer = address();
        recordRuntimeType(pointer, pointerType);
        return new InterfaceValue(pointer, pointerType);
    }
    Bool() {
        const operation = this.operations()?.bool;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Bool");
        }
        return operation(this.source);
    }
    Bytes() {
        const operations = this.operations();
        const box = this.source;
        if (operations === undefined || box === undefined) {
            return this.operationPanic("Bytes");
        }
        const bytes = operations.bytes;
        if (bytes === undefined) {
            const type = this.resolvedType();
            if (type !== undefined && type.Kind().value === Slice.value) {
                return GoPanic.raise(new ProviderError("reflect.Value.Bytes of non-byte slice"));
            }
            return this.operationPanic("Bytes");
        }
        return bytes(box);
    }
    CanInt() {
        return this.operations()?.int !== undefined;
    }
    CanSet() {
        return this.addressable &&
            this.location !== undefined &&
            this.location.settable;
    }
    Cap() {
        const operation = this.operations()?.cap;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Cap");
        }
        return operation(this.source);
    }
    Convert(target) {
        const operations = this.operations();
        const type = this.resolvedType();
        if (operations === undefined || type === undefined) {
            return this.operationPanic("Convert");
        }
        if (target === undefined) {
            return GoPanic.raise(new ProviderError("reflect: Value.Convert(nil)"));
        }
        const targetOperations = runtimeValueOperations(target);
        const converted = targetOperations === undefined
            ? undefined
            : this.convertedBox(operations, targetOperations);
        if (converted === undefined) {
            return GoPanic.raise(new ProviderError(`reflect.Value.Convert: value of type ${type.String()} cannot be converted to type ${target.String()}`));
        }
        return new InterfaceValue(converted);
    }
    convertedBox(operations, target) {
        const box = this.source;
        if (box === undefined) {
            return undefined;
        }
        if (operations.int !== undefined || operations.uint !== undefined) {
            const wide = operations.int !== undefined
                ? operations.int(box)
                : operations.uint(box);
            if (target.boxInt !== undefined) {
                return target.boxInt(wide);
            }
            if (target.boxUint !== undefined) {
                return target.boxUint(wide);
            }
            if (target.boxFloat !== undefined) {
                return target.boxFloat(globalThis.Number(wide));
            }
            if (target.boxString !== undefined) {
                return target.boxString(stringFromRune(wide));
            }
            return undefined;
        }
        if (operations.float !== undefined) {
            const wide = operations.float(box);
            if (target.boxFloat !== undefined) {
                return target.boxFloat(wide);
            }
            if (target.boxInt !== undefined) {
                return target.boxInt(globalThis.BigInt(globalThis.Math.trunc(wide)));
            }
            if (target.boxUint !== undefined) {
                return target.boxUint(globalThis.BigInt(globalThis.Math.trunc(wide)));
            }
            return undefined;
        }
        if (operations.string !== undefined) {
            if (target.boxString !== undefined) {
                return target.boxString(operations.string(box));
            }
            return undefined;
        }
        return undefined;
    }
    Elem() {
        const source = this.source;
        const type = this.resolvedType();
        if (type !== undefined && type.Kind().value === Interface.value) {
            return source === undefined
                ? new InterfaceValue(undefined)
                : new InterfaceValue(source);
        }
        const operation = this.operations()?.elem;
        if (operation === undefined || source === undefined) {
            return this.operationPanic("Elem");
        }
        const location = operation(source);
        return location === undefined
            ? new LocatedValue(undefined)
            : Value.located(location);
    }
    Field(index) {
        const operations = this.operations();
        const field = operations?.field;
        const count = operations?.numField;
        if (field === undefined ||
            count === undefined ||
            this.source === undefined) {
            return this.operationPanic("Field");
        }
        if (index < 0n || index >= count) {
            return GoPanic.raise(new ProviderError("reflect: Field index out of range"));
        }
        const location = field(this.source, index);
        return new LocatedValue(location.get(), location, this.addressable);
    }
    Float() {
        const operation = this.operations()?.float;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Float");
        }
        return operation(this.source);
    }
    Grow(count) {
        const target = this.settableLocation("Grow");
        const grown = this.operations()?.grown;
        const box = this.source;
        if (grown === undefined || box === undefined) {
            return this.operationPanic("Grow");
        }
        if (count < 0n) {
            return GoPanic.raise(new ProviderError("reflect.Value.Grow: negative len"));
        }
        target.set(grown(box, count));
    }
    Index(index) {
        const operation = this.operations()?.index;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Index");
        }
        if (index < 0n || index >= this.Len()) {
            return GoPanic.raise(new ProviderError("reflect: slice index out of range"));
        }
        const location = operation(this.source, index);
        return new LocatedValue(location.get(), location, true);
    }
    Int() {
        const operation = this.operations()?.int;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Int");
        }
        return operation(this.source);
    }
    Interface() {
        const box = this.source;
        if (!this.IsValid()) {
            return GoPanic.raise(new ProviderError("reflect: call of reflect.Value.Interface on zero Value"));
        }
        const cloned = this.operations()?.cloned;
        if (cloned !== undefined && box !== undefined) {
            return cloned(box);
        }
        const type = this.resolvedType();
        if (this.location !== undefined &&
            type !== undefined &&
            type.Kind().value === Struct.value) {
            // An aliasing view must never masquerade as the Go copy.
            return GoPanic.raise(new ProviderError(`reflect: Value.Interface of ${type.String()} requires a generated clone facet`));
        }
        return box;
    }
    IsNil() {
        const type = this.resolvedType();
        if (type !== undefined && type.Kind().value === Interface.value) {
            return this.source === undefined;
        }
        const operation = this.operations()?.isNil;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("IsNil");
        }
        return operation(this.source);
    }
    IsValid() {
        return this.location !== undefined ||
            this.staticType !== undefined ||
            this.source !== undefined;
    }
    IsZero() {
        const operations = this.operations();
        const box = this.source;
        const type = this.resolvedType();
        if (type !== undefined && type.Kind().value === Interface.value) {
            return box === undefined;
        }
        if (box === undefined) {
            return this.operationPanic("IsZero");
        }
        if (operations?.isZero !== undefined) {
            return operations.isZero(box);
        }
        if (operations?.isNil !== undefined) {
            return operations.isNil(box);
        }
        if (operations?.numField !== undefined && operations.field !== undefined) {
            for (let index = 0n; index < operations.numField; index++) {
                if (!this.Field(index).IsZero()) {
                    return false;
                }
            }
            return true;
        }
        return this.operationPanic("IsZero");
    }
    Kind() {
        const type = this.resolvedType();
        if (type === undefined) {
            if (this.source === undefined) {
                return Invalid;
            }
            return GoPanic.raiseRuntime("reflect: value type has no registered canonical descriptor");
        }
        return type.Kind();
    }
    Len() {
        const operation = this.operations()?.len;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Len");
        }
        return operation(this.source);
    }
    MapIndex(key) {
        const operation = this.operations()?.mapIndex;
        const box = this.source;
        if (operation === undefined ||
            box === undefined ||
            !key.IsValid()) {
            return this.operationPanic("MapIndex");
        }
        const [value, present] = operation(box, key.source);
        if (!present) {
            return new InterfaceValue(undefined);
        }
        const elementType = this.resolvedType()?.Elem();
        if (elementType === undefined) {
            return this.operationPanic("MapIndex");
        }
        return new InterfaceValue(value, elementType);
    }
    MapRange() {
        const operations = this.operations();
        const keys = operations?.mapKeys;
        const lookup = operations?.mapIndex;
        const box = this.source;
        const mapType = this.resolvedType();
        const keyType = mapType?.Key();
        const elementType = mapType?.Elem();
        if (keys === undefined ||
            lookup === undefined ||
            box === undefined ||
            keyType === undefined ||
            elementType === undefined) {
            return this.operationPanic("MapRange");
        }
        return new MapIterator(keys(box), (key) => lookup(box, key), keyType, elementType);
    }
    NumField() {
        const count = this.operations()?.numField;
        if (count === undefined || this.source === undefined) {
            return this.operationPanic("NumField");
        }
        return count;
    }
    Set(value) {
        const target = this.settableLocation("Set");
        if (!value.IsValid()) {
            return GoPanic.raise(new ProviderError("reflect: Set using zero Value argument"));
        }
        const payload = target.type().Kind().value === Interface.value
            ? value.Interface()
            : value.source;
        target.set(payload);
    }
    settableLocation(operation) {
        if (this.location === undefined || !this.IsValid()) {
            return this.operationPanic(operation);
        }
        if (!this.addressable || !this.location.settable) {
            return GoPanic.raise(new ProviderError(`reflect: reflect.Value.${operation} using unaddressable value`));
        }
        return this.location;
    }
    SetBool(value) {
        const target = this.settableLocation("SetBool");
        const box = this.operations()?.boxBool;
        if (box === undefined) {
            return this.operationPanic("SetBool");
        }
        target.set(box(value));
    }
    SetBytes(value) {
        const target = this.settableLocation("SetBytes");
        const operations = this.operations();
        const box = this.source;
        if (operations === undefined || box === undefined) {
            return this.operationPanic("SetBytes");
        }
        const boxBytes = operations.boxBytes;
        if (boxBytes === undefined) {
            const type = this.resolvedType();
            if (type !== undefined && type.Kind().value === Slice.value) {
                return GoPanic.raise(new ProviderError("reflect.Value.SetBytes of non-byte slice"));
            }
            return this.operationPanic("SetBytes");
        }
        target.set(boxBytes(value));
    }
    SetFloat(value) {
        const target = this.settableLocation("SetFloat");
        const box = this.operations()?.boxFloat;
        if (box === undefined) {
            return this.operationPanic("SetFloat");
        }
        target.set(box(value));
    }
    SetInt(value) {
        const target = this.settableLocation("SetInt");
        const box = this.operations()?.boxInt;
        if (box === undefined) {
            return this.operationPanic("SetInt");
        }
        target.set(box(value));
    }
    SetIterKey(iterator) {
        const target = this.settableLocation("SetIterKey");
        const state = mapIteratorState(iterator, "SetIterKey");
        target.set(state.currentKey());
    }
    SetIterValue(iterator) {
        const target = this.settableLocation("SetIterValue");
        const state = mapIteratorState(iterator, "SetIterValue");
        const [value, present] = state.currentValue();
        if (!present) {
            return GoPanic.raise(new ProviderError("reflect: map entry deleted during iteration"));
        }
        target.set(value);
    }
    SetLen(length) {
        const target = this.settableLocation("SetLen");
        const operations = this.operations();
        const resliced = operations?.resliced;
        const capacity = operations?.cap;
        const box = this.source;
        if (resliced === undefined ||
            capacity === undefined ||
            box === undefined) {
            return this.operationPanic("SetLen");
        }
        if (length < 0n || length > capacity(box)) {
            return GoPanic.raise(new ProviderError("reflect: slice length out of range in SetLen"));
        }
        target.set(resliced(box, length));
    }
    SetMapIndex(key, element) {
        const operation = this.operations()?.mapStore;
        const box = this.source;
        if (operation === undefined ||
            box === undefined ||
            !key.IsValid()) {
            return this.operationPanic("SetMapIndex");
        }
        operation(box, key.source, element.source, !element.IsValid());
    }
    SetString(value) {
        const target = this.settableLocation("SetString");
        const box = this.operations()?.boxString;
        if (box === undefined) {
            return this.operationPanic("SetString");
        }
        target.set(box(value));
    }
    SetUint(value) {
        const target = this.settableLocation("SetUint");
        const box = this.operations()?.boxUint;
        if (box === undefined) {
            return this.operationPanic("SetUint");
        }
        target.set(box(value));
    }
    SetZero() {
        const target = this.settableLocation("SetZero");
        const zero = this.operations()?.zero;
        if (zero === undefined) {
            return this.operationPanic("SetZero");
        }
        target.set(zero());
    }
    String() {
        if (!this.IsValid()) {
            return "<invalid Value>";
        }
        const source = this.source;
        const operation = this.operations()?.string;
        if (operation !== undefined && source !== undefined) {
            return operation(source);
        }
        const type = this.resolvedType();
        return type === undefined
            ? "<invalid Value>"
            : `<${type.String()} Value>`;
    }
    Type() {
        if (!this.IsValid()) {
            return GoPanic.raise(new ProviderError("reflect: call of reflect.Value.Type on zero Value"));
        }
        const type = this.resolvedType();
        if (type === undefined) {
            return GoPanic.raiseRuntime("reflect: value type has no registered canonical descriptor");
        }
        return type;
    }
    Uint() {
        const operation = this.operations()?.uint;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("Uint");
        }
        return operation(this.source);
    }
    UnsafePointer() {
        const operation = this.operations()?.unsafePointer;
        if (operation === undefined || this.source === undefined) {
            return this.operationPanic("UnsafePointer");
        }
        return operation(this.source);
    }
}
class InterfaceValue extends Value {
    constructor(source, type) {
        super(source, undefined, false, type);
    }
}
class LocatedValue extends Value {
    constructor(source, location, addressable = false) {
        super(source, location, addressable);
    }
}
export function ValueOf(value) {
    return new InterfaceValue(value);
}
class MapIterator {
    keys;
    valueAt;
    keyType;
    elementType;
    position = -1;
    constructor(keys, valueAt, keyType, elementType) {
        this.keys = keys;
        this.valueAt = valueAt;
        this.keyType = keyType;
        this.elementType = elementType;
    }
    $copy() {
        const result = new MapIterator(this.keys, this.valueAt, this.keyType, this.elementType);
        result.position = this.position;
        return result;
    }
    currentKey() {
        return this.keys[this.position];
    }
    currentValue() {
        return this.valueAt(this.currentKey());
    }
}
// mapIteratorState validates one positioned live iterator with the exact
// Go panic messages for premature and exhausted access.
function mapIteratorState(receiver, operation) {
    if (receiver === undefined) {
        return GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    if (receiver.position < 0) {
        return GoPanic.raise(new ProviderError(`MapIter.${operation} called before Next`));
    }
    if (receiver.position >= receiver.keys.length) {
        return GoPanic.raise(new ProviderError(`MapIter.${operation} called on exhausted iterator`));
    }
    return receiver;
}
export const MapIter = Object.freeze({
    Key(receiver) {
        const state = mapIteratorState(receiver, "Key");
        return new InterfaceValue(state.currentKey(), state.keyType);
    },
    Next(receiver) {
        if (receiver === undefined) {
            return GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
        }
        if (receiver.position >= receiver.keys.length) {
            return GoPanic.raise(new ProviderError("MapIter.Next called on exhausted iterator"));
        }
        receiver.position++;
        while (receiver.position < receiver.keys.length &&
            !receiver.currentValue()[1]) {
            receiver.position++;
        }
        return receiver.position < receiver.keys.length;
    },
    Value(receiver) {
        const state = mapIteratorState(receiver, "Value");
        const [value, present] = state.currentValue();
        if (!present) {
            return GoPanic.raise(new ProviderError("reflect: map entry deleted during iteration"));
        }
        return new InterfaceValue(value, state.elementType);
    },
});
export class Method {
    Name;
    PkgPath;
    Type;
    Func;
    Index;
    constructor(fields) {
        this.Name = fields.Name;
        this.PkgPath = fields.PkgPath;
        this.Type = fields.Type;
        this.Func = fields.Func;
        this.Index = fields.Index;
    }
}
export class StructField {
    Name;
    PkgPath;
    Type;
    Tag;
    Offset;
    Index;
    Anonymous;
    constructor(fields) {
        this.Name = fields.Name;
        this.PkgPath = fields.PkgPath;
        this.Type = fields.Type;
        this.Tag = fields.Tag;
        this.Offset = fields.Offset;
        this.Index = fields.Index;
        this.Anonymous = fields.Anonymous;
    }
    IsExported() {
        return this.PkgPath === "";
    }
}
export class StructTag {
    value;
    constructor(value) {
        this.value = value;
    }
    Get(key) {
        return getStructTag(this.value, key);
    }
    Lookup(key) {
        return lookupStructTag(this.value, key);
    }
}
export function Append(slice, values) {
    return Value.$append(slice, values);
}
export function DeepEqual(left, right) {
    if (left === undefined || right === undefined) {
        return left === right;
    }
    return deepValueEqual(left, right, new globalThis.Map());
}
function deepValueEqual(left, right, visited) {
    const leftType = resolveRuntimeType(left);
    const rightType = resolveRuntimeType(right);
    if (leftType === undefined || rightType === undefined) {
        return GoPanic.raise(new ProviderError("reflect: DeepEqual requires generated reflection metadata for both operands"));
    }
    if (leftType !== rightType) {
        return false;
    }
    const seen = visited.get(left);
    if (seen !== undefined && seen.has(right)) {
        return true;
    }
    const pairs = seen ?? new globalThis.Set();
    pairs.add(right);
    visited.set(left, pairs);
    const leftValue = new InterfaceValue(left);
    const rightValue = new InterfaceValue(right);
    const kind = leftType.Kind().value;
    const operations = runtimeValueOperations(leftType);
    if (operations === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: DeepEqual requires a generated value facet for ${leftType.String()}`));
    }
    if (operations.int !== undefined) {
        return operations.int(left) === operations.int(right);
    }
    if (operations.uint !== undefined) {
        return operations.uint(left) === operations.uint(right);
    }
    if (operations.float !== undefined) {
        return operations.float(left) === operations.float(right);
    }
    if (operations.bool !== undefined) {
        return operations.bool(left) === operations.bool(right);
    }
    if (operations.string !== undefined) {
        return operations.string(left) === operations.string(right);
    }
    if (kind === Pointer.value) {
        if (leftValue.IsNil() && rightValue.IsNil()) {
            return true;
        }
        if (leftValue.IsNil() || rightValue.IsNil()) {
            return false;
        }
        const leftElem = leftValue.Elem().Interface();
        const rightElem = rightValue.Elem().Interface();
        if (leftElem === undefined || rightElem === undefined) {
            return leftElem === rightElem;
        }
        return deepValueEqual(leftElem, rightElem, visited);
    }
    if (kind === Struct.value) {
        const count = leftValue.NumField();
        for (let index = 0n; index < count; index++) {
            const leftField = leftValue.Field(index).Interface();
            const rightField = rightValue.Field(index).Interface();
            if (leftField === undefined || rightField === undefined) {
                if (leftField !== rightField) {
                    return false;
                }
                continue;
            }
            if (!deepValueEqual(leftField, rightField, visited)) {
                return false;
            }
        }
        return true;
    }
    if (kind === Slice.value) {
        if (leftValue.IsNil() !== rightValue.IsNil()) {
            return false;
        }
        const length = leftValue.Len();
        if (length !== rightValue.Len()) {
            return false;
        }
        for (let index = 0n; index < length; index++) {
            const leftElement = leftValue.Index(index).Interface();
            const rightElement = rightValue.Index(index).Interface();
            if (leftElement === undefined || rightElement === undefined) {
                if (leftElement !== rightElement) {
                    return false;
                }
                continue;
            }
            if (!deepValueEqual(leftElement, rightElement, visited)) {
                return false;
            }
        }
        return true;
    }
    if (kind === Map.value) {
        if (leftValue.IsNil() !== rightValue.IsNil()) {
            return false;
        }
        if (leftValue.Len() !== rightValue.Len()) {
            return false;
        }
        const iterator = leftValue.MapRange();
        while (MapIter.Next(iterator)) {
            const key = MapIter.Key(iterator);
            const leftElement = MapIter.Value(iterator).Interface();
            const rightEntry = rightValue.MapIndex(key);
            if (!rightEntry.IsValid()) {
                return false;
            }
            const rightElement = rightEntry.Interface();
            if (leftElement === undefined || rightElement === undefined) {
                if (leftElement !== rightElement) {
                    return false;
                }
                continue;
            }
            if (!deepValueEqual(leftElement, rightElement, visited)) {
                return false;
            }
        }
        return true;
    }
    return GoPanic.raise(new ProviderError(`reflect: DeepEqual is not supported for kind ${leftType.Kind().String()}`));
}
export function Indirect(value) {
    if (value.Kind().value !== Pointer.value) {
        return value;
    }
    return value.Elem();
}
export function MakeMap(type) {
    if (type === undefined || type.Kind().value !== Map.value) {
        return GoPanic.raise(new ProviderError("reflect: MakeMap of non-map type"));
    }
    const operation = runtimeValueOperations(type)?.makeMap;
    if (operation === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: MakeMap requires a generated value facet for ${type.String()}`));
    }
    return new InterfaceValue(operation());
}
export function MakeSlice(type, length, capacity) {
    if (type === undefined || type.Kind().value !== Slice.value) {
        return GoPanic.raise(new ProviderError("reflect: MakeSlice of non-slice type"));
    }
    if (length < 0n) {
        return GoPanic.raise(new ProviderError("reflect: negative len argument in call to reflect.MakeSlice"));
    }
    if (capacity < 0n) {
        return GoPanic.raise(new ProviderError("reflect: negative cap argument in call to reflect.MakeSlice"));
    }
    if (length > capacity) {
        return GoPanic.raise(new ProviderError("reflect: len > cap in call to reflect.MakeSlice"));
    }
    const operation = runtimeValueOperations(type)?.makeSlice;
    if (operation === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: MakeSlice requires a generated value facet for ${type.String()}`));
    }
    return new InterfaceValue(operation(length, capacity));
}
export function MapOf(key, element) {
    return GoPanic.raise(new ProviderError(`reflect: MapOf requires a generated descriptor for map[${key === undefined ? "?" : key.String()}]${element === undefined ? "?" : element.String()}`));
}
export function New(type) {
    if (type === undefined) {
        return GoPanic.raise(new ProviderError("reflect: New(nil)"));
    }
    const pointerType = pointerDescriptorFor(type);
    const operation = pointerType === undefined
        ? undefined
        : runtimeValueOperations(pointerType)?.newPointer;
    if (operation === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: New requires a generated pointer facet for ${type.String()}`));
    }
    return new InterfaceValue(operation());
}
export function PointerTo(type) {
    if (type === undefined) {
        return GoPanic.raise(new ProviderError("reflect: PointerTo(nil)"));
    }
    const pointerType = pointerDescriptorFor(type);
    if (pointerType === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: PointerTo requires a generated descriptor for *${type.String()}`));
    }
    return pointerType;
}
export function SliceOf(type) {
    return GoPanic.raise(new ProviderError(`reflect: SliceOf requires a generated descriptor for []${type === undefined ? "?" : type.String()}`));
}
export function TypeAssert(_value) {
    return providerPlaceholder("reflect.TypeAssert requires generated reflection metadata");
}
export function TypeFor() {
    return providerPlaceholder("reflect.TypeFor requires generated reflection metadata");
}
export function TypeOf(_value) {
    return providerPlaceholder("reflect.TypeOf requires generated reflection metadata");
}
export function Zero(type) {
    if (type === undefined) {
        return GoPanic.raise(new ProviderError("reflect: Zero(nil)"));
    }
    const operation = runtimeValueOperations(type)?.zero;
    if (operation === undefined) {
        return GoPanic.raise(new ProviderError(`reflect: Zero requires a generated value facet for ${type.String()}`));
    }
    return new InterfaceValue(operation(), type);
}
// stringFromRune is the exact Go integer-to-string conversion: one rune,
// with values outside the valid Unicode code point range replaced by
// U+FFFD.
function stringFromRune(value) {
    const code = globalThis.Number(value);
    if (!globalThis.Number.isInteger(code) ||
        code < 0 ||
        code > 0x10ffff ||
        (code >= 0xd800 && code <= 0xdfff)) {
        return "\uFFFD";
    }
    return globalThis.String.fromCodePoint(code);
}
const kindNames = [
    "invalid", "bool", "int", "int8", "int16", "int32", "int64", "uint",
    "uint8", "uint16", "uint32", "uint64", "uintptr", "float32", "float64",
    "complex64", "complex128", "array", "chan", "func", "interface", "map",
    "ptr", "slice", "string", "struct", "unsafe.Pointer",
];
