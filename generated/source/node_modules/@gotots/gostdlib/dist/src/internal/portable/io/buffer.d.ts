import type { GoError } from "@gotots/runtime/interface-value.js";
import { RuntimeSlice } from "@gotots/runtime/slice.js";
import type { int, uint8 } from "@gotots/gostdlib/internal/scalars.js";
import type { Reader, Writer } from "../../../io.js";
export declare class BufferedReaderState {
    private readonly source;
    private readonly buffered;
    private pendingFailure;
    constructor(source: Reader);
    Read(destination: RuntimeSlice<uint8>): [int, GoError | undefined];
    ReadByte(): [uint8, GoError | undefined];
    ReadBytes(delimiter: uint8): [RuntimeSlice<uint8>, GoError | undefined];
    private fill;
    private takeFailure;
}
export declare class BufferedWriterState {
    private readonly target;
    private readonly buffered;
    private pendingFailure;
    constructor(target: Writer);
    Flush(): GoError | undefined;
    Write(source: RuntimeSlice<uint8>): [int, GoError | undefined];
    WriteByte(value: uint8): GoError | undefined;
}
