import { GoInterfaceValue } from "@gotots/runtime/interface-value.js";
import { GoPanic } from "@gotots/runtime/panic.js";
export class ProviderInterfaceValue extends GoInterfaceValue {
    $go$type;
    $go$methods = new Set();
    $go$formatString = false;
    constructor($go$type) {
        super();
        this.$go$type = $go$type;
    }
    $go$implements(contract) {
        return contract.every((token) => this.$go$methods.has(token));
    }
    $go$equal(other) {
        return this === other;
    }
    $go$hash() {
        return 0;
    }
    $go$format(_verb, _flags, _precision) {
        return GoPanic.raiseRuntime("provider value has no formatting contract");
    }
}
