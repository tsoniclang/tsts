import { endianness } from "node:os";
import { BigEndianOrder, LittleEndianOrder, } from "../../../portable/encoding/binary/byte-order.js";
export function nativeEndian() {
    return endianness() === "BE" ? new BigEndianOrder() : new LittleEndianOrder();
}
