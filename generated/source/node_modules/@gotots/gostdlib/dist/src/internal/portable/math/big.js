import { GoPanic } from "@gotots/runtime/panic.js";
import { hostInteger } from "../../host-integer.js";
const accuracyBelow = -1;
const accuracyExact = 0;
const accuracyAbove = 1;
const intValues = new WeakMap();
const floatValues = new WeakMap();
export class Accuracy {
    value;
    constructor(value) {
        this.value = value;
    }
}
export class Int {
    constructor(value = 0n) {
        intValues.set(this, value);
    }
    static Exp(receiver, base, exponent, modulus) {
        const target = requireInt(receiver);
        const sourceBase = intValue(requireInt(base));
        const sourceExponent = intValue(requireInt(exponent));
        const sourceModulus = modulus === undefined ? 0n : intValue(modulus);
        if (sourceExponent < 0n && sourceModulus !== 0n) {
            const inverse = modularInverse(sourceBase, absolute(sourceModulus));
            if (inverse === undefined) {
                return undefined;
            }
            intValues.set(target, modularPower(inverse, -sourceExponent, absolute(sourceModulus)));
            return target;
        }
        if (sourceExponent <= 0n) {
            intValues.set(target, 1n);
            return target;
        }
        if (sourceModulus === 0n) {
            intValues.set(target, sourceBase ** sourceExponent);
            return target;
        }
        intValues.set(target, modularPower(sourceBase, sourceExponent, absolute(sourceModulus)));
        return target;
    }
    static Float64(receiver) {
        return integerFloat64(intValue(requireInt(receiver)));
    }
    static SetString(receiver, text, base) {
        const target = requireInt(receiver);
        const parsed = parseInteger(text, base);
        if (parsed === undefined) {
            return [undefined, false];
        }
        intValues.set(target, parsed);
        return [target, true];
    }
    static String(receiver) {
        return intValue(requireInt(receiver)).toString(10);
    }
}
export class Float {
    constructor() {
        floatValues.set(this, {
            value: 0n,
            precision: 0,
            accuracy: accuracyExact,
        });
    }
    static Float64(receiver) {
        const target = requireFloat(receiver);
        const [value, accuracy] = integerFloat64(floatState(target).value);
        return [value, accuracy];
    }
    static SetInt(receiver, source) {
        const target = requireFloat(receiver);
        const state = floatState(target);
        state.value = intValue(requireInt(source));
        if (state.precision === 0) {
            state.precision = Math.max(bitLength(state.value), 64);
            state.accuracy = accuracyExact;
            return target;
        }
        roundToPrecision(state);
        return target;
    }
    static SetPrec(receiver, precision) {
        const target = requireFloat(receiver);
        const state = floatState(target);
        const selected = Math.max(0, Math.min(hostInteger(precision), 4_294_967_295));
        state.precision = selected;
        if (selected === 0) {
            state.accuracy = state.value < 0n
                ? accuracyAbove
                : state.value > 0n
                    ? accuracyBelow
                    : accuracyExact;
            state.value = 0n;
            return target;
        }
        roundToPrecision(state);
        return target;
    }
}
export function NewInt(value) {
    return new Int(value);
}
export function intRepresentationAssign(target, source) {
    intValues.set(target, intValue(source));
}
export function intRepresentationCopy(source) {
    return new Int(intValue(source));
}
export function floatRepresentationAssign(target, source) {
    const state = floatState(source);
    floatValues.set(target, {
        value: state.value,
        precision: state.precision,
        accuracy: state.accuracy,
    });
}
export function floatRepresentationCopy(source) {
    const target = new Float();
    floatRepresentationAssign(target, source);
    return target;
}
function requireInt(value) {
    if (value === undefined) {
        GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    return value;
}
function requireFloat(value) {
    if (value === undefined) {
        GoPanic.raiseRuntime("invalid memory address or nil pointer dereference");
    }
    return value;
}
function intValue(value) {
    const result = intValues.get(value);
    if (result === undefined) {
        GoPanic.raiseRuntime("invalid big.Int state");
    }
    return result;
}
function floatState(value) {
    const result = floatValues.get(value);
    if (result === undefined) {
        GoPanic.raiseRuntime("invalid big.Float state");
    }
    return result;
}
function absolute(value) {
    return value < 0n ? -value : value;
}
function bitLength(value) {
    return absolute(value).toString(2).length;
}
function roundToPrecision(target) {
    const magnitude = absolute(target.value);
    const length = bitLength(magnitude);
    if (magnitude === 0n || length <= target.precision) {
        target.accuracy = accuracyExact;
        return;
    }
    const shift = BigInt(length - target.precision);
    const unit = 1n << shift;
    const quotient = magnitude / unit;
    const remainder = magnitude % unit;
    const half = unit >> 1n;
    const increment = remainder > half || (remainder === half && (quotient & 1n) === 1n);
    const roundedMagnitude = (quotient + (increment ? 1n : 0n)) * unit;
    const rounded = target.value < 0n ? -roundedMagnitude : roundedMagnitude;
    target.accuracy = rounded < target.value ? accuracyBelow : accuracyAbove;
    target.value = rounded;
}
function integerFloat64(value) {
    const result = Number(value);
    if (!Number.isFinite(result)) {
        return [result, new Accuracy(value < 0n ? accuracyBelow : accuracyAbove)];
    }
    const represented = BigInt(result);
    if (represented === value) {
        return [result, new Accuracy(accuracyExact)];
    }
    return [
        result,
        new Accuracy(represented < value ? accuracyBelow : accuracyAbove),
    ];
}
function modularPower(base, exponent, modulus) {
    if (modulus === 1n) {
        return 0n;
    }
    let factor = ((base % modulus) + modulus) % modulus;
    let power = exponent;
    let result = 1n;
    while (power > 0n) {
        if ((power & 1n) === 1n) {
            result = (result * factor) % modulus;
        }
        factor = (factor * factor) % modulus;
        power >>= 1n;
    }
    return result;
}
function modularInverse(value, modulus) {
    let oldRemainder = ((value % modulus) + modulus) % modulus;
    let remainder = modulus;
    let oldCoefficient = 1n;
    let coefficient = 0n;
    while (remainder !== 0n) {
        const quotient = oldRemainder / remainder;
        [oldRemainder, remainder] = [remainder, oldRemainder - quotient * remainder];
        [oldCoefficient, coefficient] = [coefficient, oldCoefficient - quotient * coefficient];
    }
    if (oldRemainder !== 1n) {
        return undefined;
    }
    return ((oldCoefficient % modulus) + modulus) % modulus;
}
function parseInteger(text, requestedBase) {
    if (requestedBase !== 0n && (requestedBase < 2n || requestedBase > 62n)) {
        GoPanic.raiseRuntime("invalid number base");
    }
    if (text.length === 0 || text.trim() !== text) {
        return undefined;
    }
    let index = 0;
    let negative = false;
    if (text[0] === "+" || text[0] === "-") {
        negative = text[0] === "-";
        index += 1;
    }
    if (index === text.length) {
        return undefined;
    }
    let base = hostInteger(requestedBase);
    let prefix = false;
    if (base === 0) {
        base = 10;
        if (text[index] === "0") {
            const marker = text[index + 1];
            if (marker === "b" || marker === "B") {
                base = 2;
                index += 2;
                prefix = true;
            }
            else if (marker === "o" || marker === "O") {
                base = 8;
                index += 2;
                prefix = true;
            }
            else if (marker === "x" || marker === "X") {
                base = 16;
                index += 2;
                prefix = true;
            }
            else {
                base = 8;
            }
        }
    }
    let sawDigit = false;
    let previousUnderscore = false;
    let result = 0n;
    for (; index < text.length; index += 1) {
        const character = text[index] ?? "";
        if (character === "_") {
            if (requestedBase !== 0n || previousUnderscore || (!sawDigit && !prefix)) {
                return undefined;
            }
            previousUnderscore = true;
            prefix = false;
            continue;
        }
        const digit = digitValue(character, base);
        if (digit < 0 || digit >= base) {
            return undefined;
        }
        result = result * BigInt(base) + BigInt(digit);
        sawDigit = true;
        previousUnderscore = false;
        prefix = false;
    }
    if (!sawDigit || previousUnderscore) {
        return undefined;
    }
    return negative ? -result : result;
}
function digitValue(character, base) {
    const code = character.charCodeAt(0);
    if (code >= 48 && code <= 57) {
        return code - 48;
    }
    if (code >= 97 && code <= 122) {
        return code - 97 + 10;
    }
    if (code >= 65 && code <= 90) {
        return base <= 36 ? code - 65 + 10 : code - 65 + 36;
    }
    return -1;
}
