export function Or(copy, equal, fromStorage, zero, values) {
    const zeroValue = zero();
    for (let index = 0; index < values.length; index += 1) {
        const value = copy(fromStorage(values.get(index)));
        if (!equal(value, zeroValue)) {
            return value;
        }
    }
    return zeroValue;
}
