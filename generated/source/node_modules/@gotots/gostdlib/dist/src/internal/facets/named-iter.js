import { Seq, Seq2 } from "../../iter.js";
export class IterSeqValueOperations {
    static $project(source) {
        return source.value;
    }
    static $wrap(source) {
        return new Seq(source);
    }
}
export class IterSeq2ValueOperations {
    static $project(source) {
        return source.value;
    }
    static $wrap(source) {
        return new Seq2(source);
    }
}
