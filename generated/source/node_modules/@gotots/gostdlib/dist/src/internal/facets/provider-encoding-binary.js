import { Read, Write, } from "../../encoding/binary.js";
export function EncodingBinaryReadDirect(reader, order, data) {
    return Read(reader, order, data);
}
export function EncodingBinaryWriteDirect(writer, order, data) {
    return Write(writer, order, data);
}
