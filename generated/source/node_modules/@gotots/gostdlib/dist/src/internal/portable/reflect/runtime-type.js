import { GoInterfaceValue, } from "@gotots/runtime/interface-value.js";
import { RuntimeSlice } from "@gotots/runtime/slice.js";
import { GoPanic } from "@gotots/runtime/panic.js";
import { hostInteger, integerFromHost } from "../../host-integer.js";
import { Seq } from "../../../iter.js";
import { ChanDir, Invalid, Kind, Method, StructField, StructTag, Value, } from "../../../reflect.js";
import { ProviderError } from "../../runtime/error.js";
import { bindRuntimeTypeResolver, bindRuntimeTypeRecorder, bindRuntimePointerTypeFactory, recordPointerDescriptor, } from "./runtime-value.js";
const providerPointerSize = 8n;
const providerPointerAlign = 8n;
const methodIdentityWidth = 14;
const runtimeTypeDynamicType = Object.freeze({ comparable: true });
const runtimeTypesByDynamicType = new Map();
export class RuntimeType extends GoInterfaceValue {
    metadataFactory;
    methodTokenFactory;
    registration;
    $go$type = runtimeTypeDynamicType;
    $go$formatString = true;
    resolvedMetadata;
    resolvedMethodTokens;
    resolvedMethods;
    constructor(metadataFactory, methodTokenFactory, registration) {
        super();
        this.metadataFactory = metadataFactory;
        this.methodTokenFactory = methodTokenFactory;
        this.registration = registration;
    }
    get $go$methods() {
        this.resolvedMethods ??= new Set(this.methodTokens());
        return this.resolvedMethods;
    }
    get metadata() {
        this.resolvedMetadata ??= this.metadataFactory();
        return this.resolvedMetadata;
    }
    get sourceMethodSet() {
        return this.metadata.methodSet ?? "";
    }
    methodTokens() {
        this.resolvedMethodTokens ??= this.methodTokenFactory();
        return this.resolvedMethodTokens;
    }
    $go$implements(contract) {
        return contract.every((token) => this.$go$methods.has(token));
    }
    $go$equal(other) {
        return this === other;
    }
    $go$hash() {
        let hash = 2166136261;
        for (let index = 0; index < this.metadata.identity.length; index++) {
            hash ^= this.metadata.identity.charCodeAt(index);
            hash = Math.imul(hash, 16777619);
        }
        return hash >>> 0;
    }
    $go$format(verb) {
        return verb === "T" ? "reflect.rtype" : this.metadata.text;
    }
    Align() { return this.metadata.align; }
    AssignableTo(target) {
        return this.identical(target);
    }
    Bits() {
        const bits = this.metadata.bits ?? 0n;
        if (bits === 0n) {
            return invalidTypeOperation(this.metadata.text, "Bits");
        }
        return bits;
    }
    CanSeq() {
        return [17n, 18n, 21n, 22n, 23n, 24n].includes(this.metadata.kind);
    }
    CanSeq2() {
        return [17n, 21n, 23n, 24n].includes(this.metadata.kind);
    }
    ChanDir() {
        if (this.metadata.kind !== 18n) {
            return invalidTypeOperation(this.metadata.text, "ChanDir");
        }
        return new ChanDir(this.metadata.chanDir ?? 0n);
    }
    Comparable() { return this.metadata.comparable ?? true; }
    ConvertibleTo(target) {
        return this.identical(target);
    }
    Elem() {
        return this.registration?.pointerElement?.() ??
            this.metadata.elem?.() ??
            invalidTypeOperation(this.metadata.text, "Elem");
    }
    Field(index) {
        const fields = this.structFields();
        const ordinal = hostInteger(index);
        const selected = fields[ordinal];
        if (selected === undefined) {
            return invalidTypeOperation(this.metadata.text, "Field");
        }
        return materializeField(selected, ordinal);
    }
    FieldAlign() { return this.metadata.align; }
    FieldByIndex(index) {
        let current = this;
        let selected;
        for (let position = 0; position < index.length; position++) {
            selected = current.Field(index.get(position));
            if (position + 1 < index.length) {
                current = selected.Type?.Elem() ?? selected.Type ??
                    invalidTypeOperation(this.metadata.text, "FieldByIndex");
            }
        }
        return selected ?? invalidTypeOperation(this.metadata.text, "FieldByIndex");
    }
    FieldByName(name) {
        const fields = this.structFields();
        const index = fields.findIndex((field) => field.name === name);
        const selected = fields[index];
        return selected === undefined
            ? [zeroStructField(), false]
            : [materializeField(selected, index), true];
    }
    FieldByNameFunc(match) {
        if (match === undefined) {
            return invalidTypeOperation(this.metadata.text, "FieldByNameFunc");
        }
        const fields = this.structFields();
        const index = fields.findIndex((field) => match(field.name));
        const selected = fields[index];
        return selected === undefined
            ? [zeroStructField(), false]
            : [materializeField(selected, index), true];
    }
    Fields() {
        const fields = this.structFields();
        return new Seq((yieldValue) => {
            if (yieldValue === undefined)
                return;
            for (let index = 0; index < fields.length; index++) {
                const field = fields[index];
                if (field !== undefined &&
                    !yieldValue(materializeField(field, index)))
                    return;
            }
        });
    }
    Implements(target) {
        if (!(target instanceof RuntimeType) || target.metadata.kind !== 20n) {
            return invalidTypeOperation(this.metadata.text, "Implements");
        }
        return methodSetIncludes(this.sourceMethodSet, target.sourceMethodSet);
    }
    In(index) { return sequenceAt(this.inputs(), index, "In"); }
    Ins() { return typeSequence(this.inputs()); }
    IsVariadic() { return this.metadata.variadic ?? false; }
    Key() {
        return this.metadata.key?.() ?? invalidTypeOperation(this.metadata.text, "Key");
    }
    Kind() { return this.metadata.kind === 0n ? Invalid : new Kind(this.metadata.kind); }
    Len() {
        if (this.metadata.kind !== 17n) {
            return invalidTypeOperation(this.metadata.text, "Len");
        }
        return this.metadata.length ?? 0n;
    }
    Method(index) {
        const method = this.runtimeMethods()[hostInteger(index)];
        if (method === undefined) {
            return invalidTypeOperation(this.metadata.text, "Method");
        }
        return materializeMethod(method);
    }
    MethodByName(name) {
        const method = this.runtimeMethods().find((selected) => selected.name === name);
        return method === undefined
            ? [zeroMethod(), false]
            : [materializeMethod(method), true];
    }
    Methods() {
        const methods = this.runtimeMethods();
        return new Seq((yieldValue) => {
            if (yieldValue === undefined)
                return;
            for (const method of methods) {
                if (!yieldValue(materializeMethod(method)))
                    return;
            }
        });
    }
    Name() { return this.metadata.name ?? ""; }
    NumField() { return integerFromHost(this.structFields().length); }
    NumIn() { return integerFromHost(this.inputs().length); }
    NumMethod() { return integerFromHost(this.runtimeMethods().length); }
    NumOut() { return integerFromHost(this.outputs().length); }
    Out(index) { return sequenceAt(this.outputs(), index, "Out"); }
    Outs() { return typeSequence(this.outputs()); }
    OverflowComplex(value) {
        const limit = this.metadata.kind === 15n ? 3.4028234663852886e38 : Number.MAX_VALUE;
        return Math.abs(value.real) > limit || Math.abs(value.imag) > limit;
    }
    OverflowFloat(value) {
        return this.metadata.kind === 13n && Math.abs(value) > 3.4028234663852886e38;
    }
    OverflowInt(value) {
        const bits = hostInteger(this.Bits());
        return value < -(1n << BigInt(bits - 1)) ||
            value >= (1n << BigInt(bits - 1));
    }
    OverflowUint(value) {
        const bits = hostInteger(this.Bits());
        return value < 0n || value >= (1n << BigInt(bits));
    }
    PkgPath() { return this.metadata.pkgPath ?? ""; }
    Size() { return this.metadata.size; }
    String() { return this.metadata.text; }
    inputs() { return this.metadata.inputs?.() ?? []; }
    outputs() { return this.metadata.outputs?.() ?? []; }
    runtimeMethods() {
        return this.metadata.methods?.() ?? [];
    }
    structFields() {
        if (this.metadata.kind !== 25n) {
            return invalidTypeOperation(this.metadata.text, "Field");
        }
        return this.metadata.fields ?? [];
    }
    identical(target) {
        return target instanceof RuntimeType &&
            this.metadata.identity === target.metadata.identity;
    }
    pointerType() {
        const pointerMethods = this.metadata.pointerInheritsMethods
            ? mergeMethodSets(this.sourceMethodSet, this.metadata.pointerMethodSet ?? "")
            : (this.metadata.pointerMethodSet ?? "");
        return new RuntimeType(() => ({
            identity: `pointer(${this.metadata.identity})`,
            kind: 22n,
            text: `*${this.metadata.text}`,
            size: providerPointerSize,
            align: providerPointerAlign,
            elem: () => this,
            methodSet: pointerMethods,
        }), () => this.methodTokens());
    }
}
function methodSetIncludes(source, target) {
    let sourceOffset = 0;
    let targetOffset = 0;
    while (targetOffset < target.length) {
        if (sourceOffset >= source.length) {
            return false;
        }
        const sourceIdentity = source.slice(sourceOffset, sourceOffset + methodIdentityWidth);
        const targetIdentity = target.slice(targetOffset, targetOffset + methodIdentityWidth);
        if (sourceIdentity < targetIdentity) {
            sourceOffset += methodIdentityWidth;
            continue;
        }
        if (sourceIdentity !== targetIdentity) {
            return false;
        }
        sourceOffset += methodIdentityWidth;
        targetOffset += methodIdentityWidth;
    }
    return true;
}
function mergeMethodSets(left, right) {
    let leftOffset = 0;
    let rightOffset = 0;
    let merged = "";
    while (leftOffset < left.length || rightOffset < right.length) {
        const leftIdentity = left.slice(leftOffset, leftOffset + methodIdentityWidth);
        const rightIdentity = right.slice(rightOffset, rightOffset + methodIdentityWidth);
        if (rightOffset >= right.length ||
            (leftOffset < left.length && leftIdentity < rightIdentity)) {
            merged += leftIdentity;
            leftOffset += methodIdentityWidth;
            continue;
        }
        merged += rightIdentity;
        rightOffset += methodIdentityWidth;
        if (leftIdentity === rightIdentity) {
            leftOffset += methodIdentityWidth;
        }
    }
    return merged;
}
export function createRuntimeType(metadata, methodTokens, registration) {
    const result = new RuntimeType(metadata, methodTokens, registration);
    if (registration?.dynamicType !== undefined) {
        runtimeTypesByDynamicType.set(registration.dynamicType, result);
    }
    if (registration?.pointerElement !== undefined) {
        recordPointerDescriptor(result, registration.pointerElement);
    }
    return result;
}
export function runtimeTypeOf(value) {
    return value === undefined
        ? undefined
        : runtimeTypesByDynamicType.get(value.$go$type);
}
bindRuntimeTypeResolver((value) => runtimeTypesByDynamicType.get(value.$go$type));
bindRuntimeTypeRecorder((value, type) => {
    if (!(type instanceof RuntimeType)) {
        return GoPanic.raiseRuntime("reflect: recorded runtime type is foreign");
    }
    const existing = runtimeTypesByDynamicType.get(value.$go$type);
    if (existing !== undefined && existing !== type) {
        return GoPanic.raiseRuntime("reflect: dynamic type token changed identity");
    }
    runtimeTypesByDynamicType.set(value.$go$type, type);
});
bindRuntimePointerTypeFactory((element) => element instanceof RuntimeType ? element.pointerType() : undefined);
function materializeField(field, ordinal) {
    return new StructField({
        Name: field.name,
        PkgPath: field.pkgPath ?? "",
        Type: field.type(),
        Tag: new StructTag(field.tag ?? ""),
        Offset: field.offset ?? 0n,
        Index: RuntimeSlice.literal([...(field.index ?? [integerFromHost(ordinal)])]),
        Anonymous: field.anonymous ?? false,
    });
}
function materializeMethod(method) {
    return new Method({
        Name: method.name,
        PkgPath: method.pkgPath,
        Type: method.type(),
        Func: invalidRuntimeValue,
        Index: method.index,
    });
}
class InvalidRuntimeValue extends Value {
    constructor() { super(); }
}
const invalidRuntimeValue = new InvalidRuntimeValue();
function zeroStructField() {
    return new StructField({
        Name: "",
        PkgPath: "",
        Type: undefined,
        Tag: new StructTag(""),
        Offset: 0n,
        Index: RuntimeSlice.nil(),
        Anonymous: false,
    });
}
function zeroMethod() {
    return new Method({ Name: "", PkgPath: "", Type: undefined, Func: invalidRuntimeValue, Index: 0n });
}
function sequenceAt(values, index, operation) {
    const selected = values[hostInteger(index)];
    return selected ?? invalidTypeOperation("func", operation);
}
function typeSequence(values) {
    return new Seq((yieldValue) => {
        if (yieldValue === undefined)
            return;
        for (const value of values) {
            if (!yieldValue(value))
                return;
        }
    });
}
function invalidTypeOperation(type, operation) {
    return GoPanic.raise(new ProviderError(`reflect: ${operation} of non-${type} type`));
}
