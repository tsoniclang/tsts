import { DirectGzipReader, } from "./provider-compress-gzip-direct.js";
export class GzipReaderOperations {
    static $copy(source) {
        return DirectGzipReader.$copy(source);
    }
    static $assign(target, source) {
        DirectGzipReader.$assign(target, source);
    }
}
