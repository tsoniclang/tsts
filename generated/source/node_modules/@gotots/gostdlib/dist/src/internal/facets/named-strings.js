import { Builder } from "../../strings.js";
import { builderRepresentationAssign, builderRepresentationCopy, } from "../portable/strings/builder.js";
import { readerRepresentationAssign, readerRepresentationCopy, readerRepresentationEqual, readerRepresentationHash, } from "../portable/strings/reader.js";
import { replacerRepresentationAssign, replacerRepresentationCopy, } from "../portable/strings/replacer.js";
export class StringsBuilderOperations {
    static $zero() {
        return new Builder();
    }
    static $copy(source) {
        return builderRepresentationCopy(source);
    }
    static $assign(target, source) {
        builderRepresentationAssign(target, source);
    }
    static $storageOf(source) {
        return source;
    }
    static $fromStorage(source) {
        return source;
    }
}
export class StringsReaderOperations {
    static $copy(source) {
        return readerRepresentationCopy(source);
    }
    static $assign(target, source) {
        readerRepresentationAssign(target, source);
    }
    static $equal(left, right) {
        return readerRepresentationEqual(left, right);
    }
    static $hash(source) {
        return readerRepresentationHash(source);
    }
}
export class StringsReplacerOperations {
    static $copy(source) {
        return replacerRepresentationCopy(source);
    }
    static $assign(target, source) {
        replacerRepresentationAssign(target, source);
    }
}
