export function wallMilliseconds() {
    return Date.now();
}
export function monotonicMilliseconds() {
    return performance.now();
}
export function schedule(delayMilliseconds, action) {
    return setTimeout(action, Math.max(0, delayMilliseconds));
}
export function cancelSchedule(handle) {
    clearTimeout(handle);
}
