import { ProviderInterfaceValue } from "./value.js";
const directoryFileType = Object.freeze({ comparable: true });
export class DirectoryFile extends ProviderInterfaceValue {
    constructor(type = directoryFileType) {
        super(type);
    }
}
