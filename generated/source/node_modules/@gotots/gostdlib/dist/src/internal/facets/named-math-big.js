import { Accuracy, Float, Int } from "../../math/big.js";
import { floatRepresentationAssign, floatRepresentationCopy, intRepresentationAssign, intRepresentationCopy, } from "../portable/math/big.js";
export class MathBigAccuracyValueOperations {
    static $project(source) {
        return source.value;
    }
    static $wrap(source) {
        return new Accuracy(source);
    }
}
export class MathBigFloatOperations {
    static $assign(target, source) {
        floatRepresentationAssign(target, source);
    }
    static $copy(source) {
        return floatRepresentationCopy(source);
    }
    static $zero() {
        return new Float();
    }
    static $storageOf(source) {
        return source;
    }
    static $fromStorage(source) {
        return source;
    }
}
export class MathBigIntOperations {
    static $assign(target, source) {
        intRepresentationAssign(target, source);
    }
    static $copy(source) {
        return intRepresentationCopy(source);
    }
    static $zero() {
        return new Int();
    }
    static $storageOf(source) {
        return source;
    }
    static $fromStorage(source) {
        return source;
    }
}
