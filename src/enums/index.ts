/**
 * TS-Go enum definitions (SyntaxKind, NodeFlags, TokenFlags, etc.) in TypeScript form.
 *
 * Mirrors TS-Go internal/ast/* (multiple enum sources).
 *
 * Status: empty placeholder. Adopt from @typescript/native-preview/src/enums/.
 *
 * See docs/tsgo-mapping.md for the full TS-Go ↔ TSTS file map.
 */

export {};
